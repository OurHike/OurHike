name: SCRATCH env gate check (temporary, delete before merge)

on:
  workflow_dispatch:
    inputs:
      go:
        type: boolean
        default: false

jobs:
  check:
    runs-on: ubuntu-latest
    environment: ${{ inputs.go && 'production' || 'scratch-no-write' }}
    steps:
      - run: echo "reached the job body; environment resolved to a state that did not block it"
