An unattended run of `propose_atc_updates.py` (#463) drafted `pipeline/reference/atc_updates_proposed.json` from ATC's own Trail Updates page - the residue [#963](https://github.com/OurHike/OurHike/issues/963)'s hourly gate could not publish on its own (several mile references that do not agree, a category this build does not know, or similar).

**This pull request publishes nothing.** `atc_updates_proposed.json` is not read by `export_atc_updates.py`, `publish.py`, or any client. Compare each candidate against ATC's own page (`source_url`), and if it is right, type the row into `pipeline/reference/atc_updates.json` by hand - the reviewed-file path [#460](https://github.com/OurHike/OurHike/issues/460) already proved - then close this pull request without merging it, since there is nothing here for `main` to gain by merging.

Refreshed on every scheduled run: this branch is force-pushed with the current candidate set each time, so this pull request's diff always reflects today's, not the day it first opened.

---
_Generated by [Claude Code](https://claude.ai/code)_
