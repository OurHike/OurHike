function e(e){if(e.kind===`looking`)return`Looking for the trail network…`;switch(e.because){case`unconfigured`:return`This build has no data source, so there is nothing to build a day hike on.`;case`unreachable`:return`The trail network has not downloaded yet, and it needs a connection.`;case`not-in-release`:return`This release does not include the trail network, so there is nothing to build a day hike on.`;case`empty`:return`There are no mapped trails here yet to build a day hike from.`;case`too-large`:return`The trail network in this release is too big for the app to load safely, so it is not being used.`;case`unverifiable`:case`not-a-graph`:return`The trail network this phone downloaded does not check out, so it is not being used.`}}function t(e){return e.kind===`absent`&&e.because===`unreachable`}export{e as n,t};