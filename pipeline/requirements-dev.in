-r requirements.in
pytest
pytest-cov

# Runs the suite across cores. This one is nearly all pure-function and
# synthetic-fixture tests with no shared state between them, so the split is
# free: measured here, 45s serial against 22s on four workers.
pytest-xdist

requests-mock
moto[s3]
ruff
