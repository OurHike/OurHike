// The other standing shot (see README.md, and STANDING in
// scripts/photograph-preview.mjs): the trail screen a hiker lands on past
// first run, photographed on every pull request. No drive — the app opens
// here on its own once first run is skipped, which the runner's default
// already does.
export const caption = 'The trail screen'
export const alt = 'The trail screen, past first run'
